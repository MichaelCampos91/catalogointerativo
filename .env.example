# Autenticação do painel admin
# Senha para login em /admin. Use apenas no servidor (não use NEXT_PUBLIC_ em produção).
ADMIN_PASSWORD=

# Segredo para assinar o JWT do cookie de sessão (opcional; há valor padrão em desenvolvimento).
JWT_SECRET=
